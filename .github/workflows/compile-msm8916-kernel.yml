name: Compile msm8916 kernel

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      kernel_config:
        description: "Select kernel config"
        required: false
        default: "msm8916_defconfig"
        type: choice
        options:
          - msm8916_defconfig
          - defconfig
      ccache_clear:
        description: "Set whether to clear the cache"
        required: false
        default: false
        type: boolean

env:
  TZ: Etc/UTC
  KERNEL_REPO: Crychic-Band/kernel_ufi_msm8916

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Initialize the build environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          docker rmi -f $(docker images -q) 2>/dev/null || true
          [[ -n "${AGENT_TOOLSDIRECTORY}" ]] && sudo rm -rf "${AGENT_TOOLSDIRECTORY}"
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android 2>/dev/null
          sudo swapoff -a
          sudo rm -f /swapfile /mnt/swapfile
          sudo -E apt-get -y update
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y install build-essential bc bison flex libssl-dev libncurses5-dev libelf-dev dwarves fakeroot dpkg-dev debhelper rsync kmod cpio python3 git wget curl jq ccache libdw-dev
          sudo -E systemctl daemon-reload
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean
          sudo sed -i '/NVM_DIR/d;/skel/d' /root/{.bashrc,.profile}
          sudo rm -rf ~/{.cargo,.dotnet,.rustup}
          sudo -E timedatectl set-timezone "${TZ:-Etc/UTC}"
          sudo -E ntpdate ntp.ubuntu.com 0.pool.ntp.org || true
          sudo -E timedatectl set-ntp true
          date -u
          timedatectl status || true
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Create virtual disk for extended storage
        id: disk
        run: |
          mnt_size=$(expr $(df -h /mnt | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 1)
          root_size=$(expr $(df -h / | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 4)
          sudo truncate -s "${mnt_size}"G /mnt/mnt.img
          sudo truncate -s "${root_size}"G /root.img
          sudo losetup /dev/loop6 /mnt/mnt.img
          sudo losetup /dev/loop7 /root.img
          sudo pvcreate /dev/loop6
          sudo pvcreate /dev/loop7
          sudo vgcreate github /dev/loop6 /dev/loop7
          sudo lvcreate -n runner -l 100%FREE github
          sudo mkfs.xfs -f -i sparse=0 -b size=4096 /dev/github/runner
          sudo mkdir -p /builder
          sudo mount /dev/github/runner /builder
          sudo chown -R runner:runner /builder
          df -Th
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Clone kernel source
        id: clone
        run: |
          cd /builder
          git clone --depth=1 https://github.com/${{ env.KERNEL_REPO }}.git kernel
          cd kernel

          # Get kernel version from Makefile
          KERNEL_VERSION=$(grep -E "^VERSION = " Makefile | awk '{print $3}')
          KERNEL_PATCHLEVEL=$(grep -E "^PATCHLEVEL = " Makefile | awk '{print $3}')
          KERNEL_SUBLEVEL=$(grep -E "^SUBLEVEL = " Makefile | awk '{print $3}')
          FULL_VERSION="${KERNEL_VERSION}.${KERNEL_PATCHLEVEL}.${KERNEL_SUBLEVEL}"

          echo "kernel_version=${FULL_VERSION}" >> ${GITHUB_ENV}
          echo "kernel_version=${FULL_VERSION}" >> ${GITHUB_OUTPUT}
          echo "Kernel version: ${FULL_VERSION}"
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Cache ccache
        uses: actions/cache@v5
        with:
          path: /builder/kernel/.ccache
          key: msm8916-${{ env.kernel_version }}-${{ github.sha }}
          restore-keys: |
            msm8916-${{ env.kernel_version }}-

      # ================================================================
      # ★ 步骤 5：加载基础 defconfig（从原 compile 步骤拆分出来）
      # ================================================================
      - name: Load base kernel config
        id: loadconfig
        if: ${{ steps.disk.outputs.status == 'success' && !cancelled() }}
        run: |
          cd /builder/kernel

          # Setup ccache
          export CCACHE_DIR=/builder/kernel/.ccache
          export PATH="/usr/lib/ccache:$PATH"
          ccache -M 5G
          [[ "${{ inputs.ccache_clear }}" == "true" ]] && ccache -C

          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-

          # 加载设备基础配置
          make ${{ inputs.kernel_config }}

          echo "Base config loaded: ${{ inputs.kernel_config }}"
          echo "status=success" >> ${GITHUB_OUTPUT}

      # ================================================================
      # ★ 步骤 6：注入 DAE + Docker 所需内核配置项（核心新增步骤）
      #
      # DAE 配置来源：
      #   https://github.com/daeuniverse/dae/blob/main/docs/en/README.md
      #   https://dae.v2raya.org/docs/current/quick-start/
      #
      # Docker 配置来源：
      #   https://github.com/docker/docker/blob/master/contrib/check-config.sh
      #   Docker 官方及社区 issue #14282 验证清单
      # ================================================================
      - name: Inject DAE and Docker kernel configs
        id: injectconfig
        if: ${{ steps.loadconfig.outputs.status == 'success' && !cancelled() }}
        run: |
          cd /builder/kernel
          export ARCH=arm64
          CFG="./scripts/config"

          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║         注入 DAE (eBPF) 官方要求的内核配置项             ║"
          echo "║  来源: github.com/daeuniverse/dae/blob/main/docs/en/README.md ║"
          echo "╚══════════════════════════════════════════════════════════╝"

          # ---------------------------------------------------------------
          # DAE 官方文档列出的全部必须项（逐字对应，不增不减）
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_BPF
          $CFG --enable  CONFIG_BPF_SYSCALL
          $CFG --enable  CONFIG_BPF_JIT
          $CFG --enable  CONFIG_CGROUPS
          $CFG --enable  CONFIG_KPROBES
          $CFG --enable  CONFIG_NET_INGRESS
          $CFG --enable  CONFIG_NET_EGRESS
          $CFG --module  CONFIG_NET_SCH_INGRESS      # 官方要求 =m
          $CFG --module  CONFIG_NET_CLS_BPF          # 官方要求 =m
          $CFG --enable  CONFIG_NET_CLS_ACT
          $CFG --enable  CONFIG_BPF_STREAM_PARSER
          $CFG --enable  CONFIG_DEBUG_INFO
          $CFG --disable CONFIG_DEBUG_INFO_REDUCED   # 官方要求 is not set
          $CFG --enable  CONFIG_DEBUG_INFO_BTF       # 最关键：CO-RE 支持
          $CFG --enable  CONFIG_KPROBE_EVENTS
          $CFG --enable  CONFIG_BPF_EVENTS

          # DAE 同样受益于 CGROUP_BPF（按进程/MAC 分流）
          $CFG --enable  CONFIG_CGROUP_BPF

          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║     注入 Docker 官方要求的内核配置项（必须项）           ║"
          echo "║  来源: github.com/docker/docker/contrib/check-config.sh  ║"
          echo "╚══════════════════════════════════════════════════════════╝"

          # ---------------------------------------------------------------
          # 命名空间（容器隔离核心）
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_NAMESPACES
          $CFG --enable  CONFIG_NET_NS
          $CFG --enable  CONFIG_PID_NS
          $CFG --enable  CONFIG_IPC_NS
          $CFG --enable  CONFIG_UTS_NS
          $CFG --enable  CONFIG_USER_NS             # 可选但推荐，rootless 容器需要

          # ---------------------------------------------------------------
          # Cgroup（容器资源限制）
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_CGROUP_CPUACCT
          $CFG --enable  CONFIG_CGROUP_DEVICE
          $CFG --enable  CONFIG_CGROUP_FREEZER
          $CFG --enable  CONFIG_CGROUP_SCHED
          $CFG --enable  CONFIG_CPUSETS
          $CFG --enable  CONFIG_MEMCG
          $CFG --enable  CONFIG_MEMCG_SWAP
          $CFG --enable  CONFIG_KEYS
          $CFG --enable  CONFIG_BLK_CGROUP
          $CFG --enable  CONFIG_BLK_DEV_THROTTLING
          $CFG --enable  CONFIG_CGROUP_PERF
          $CFG --enable  CONFIG_CGROUP_HUGETLB
          $CFG --enable  CONFIG_CFS_BANDWIDTH
          $CFG --enable  CONFIG_FAIR_GROUP_SCHED
          $CFG --enable  CONFIG_NET_CLS_CGROUP
          $CFG --enable  CONFIG_CGROUP_NET_PRIO

          # ---------------------------------------------------------------
          # 网络虚拟化（容器网络核心）
          # ---------------------------------------------------------------
          $CFG --module  CONFIG_VETH                # 容器虚拟网卡
          $CFG --module  CONFIG_BRIDGE              # 容器网桥
          $CFG --enable  CONFIG_BRIDGE_NETFILTER
          $CFG --enable  CONFIG_DUMMY

          # ---------------------------------------------------------------
          # Netfilter / iptables（容器 NAT 与网络策略）
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_NETFILTER
          $CFG --enable  CONFIG_NF_CONNTRACK
          $CFG --module  CONFIG_NF_NAT
          $CFG --module  CONFIG_IP_NF_IPTABLES
          $CFG --module  CONFIG_IP_NF_FILTER
          $CFG --module  CONFIG_IP_NF_MANGLE
          $CFG --module  CONFIG_IP_NF_TARGET_MASQUERADE
          $CFG --module  CONFIG_IP_NF_NAT
          $CFG --module  CONFIG_IP_NF_TARGET_REDIRECT
          $CFG --module  CONFIG_NETFILTER_XT_MATCH_ADDRTYPE
          $CFG --module  CONFIG_NETFILTER_XT_MATCH_CONNTRACK
          $CFG --module  CONFIG_NETFILTER_XT_MATCH_IPVS
          $CFG --module  CONFIG_NETFILTER_XT_MARK

          # ---------------------------------------------------------------
          # IPVS（Docker Swarm / 负载均衡）
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_IP_VS
          $CFG --enable  CONFIG_IP_VS_NFCT
          $CFG --enable  CONFIG_IP_VS_PROTO_TCP
          $CFG --enable  CONFIG_IP_VS_PROTO_UDP
          $CFG --enable  CONFIG_IP_VS_RR

          # ---------------------------------------------------------------
          # Overlay 文件系统（Docker 镜像分层存储，必须）
          # ---------------------------------------------------------------
          $CFG --module  CONFIG_OVERLAY_FS

          # ---------------------------------------------------------------
          # 其他必须项
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_POSIX_MQUEUE        # 进程通信
          $CFG --module  CONFIG_TUN                 # VPN / 虚拟网络接口
          $CFG --enable  CONFIG_SECCOMP             # 容器安全沙箱
          $CFG --enable  CONFIG_SECCOMP_FILTER

          # ---------------------------------------------------------------
          # 存储驱动支持
          # ---------------------------------------------------------------
          $CFG --enable  CONFIG_EXT4_FS
          $CFG --enable  CONFIG_EXT4_FS_POSIX_ACL
          $CFG --enable  CONFIG_EXT4_FS_SECURITY

          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║     运行 make olddefconfig 自动解决全部依赖关系          ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          make olddefconfig

          echo ""
          echo "════════════════ ✅ 配置验证结果 ════════════════"
          echo "--- DAE 官方要求的配置项 ---"
          grep -E "^CONFIG_(BPF|BPF_SYSCALL|BPF_JIT|CGROUPS|KPROBES|NET_INGRESS|NET_EGRESS|NET_SCH_INGRESS|NET_CLS_BPF|NET_CLS_ACT|BPF_STREAM_PARSER|DEBUG_INFO|DEBUG_INFO_BTF|KPROBE_EVENTS|BPF_EVENTS|CGROUP_BPF)=" .config | sort || true
          grep "CONFIG_DEBUG_INFO_REDUCED" .config || echo "✅ CONFIG_DEBUG_INFO_REDUCED is not set (correct)"
          echo ""
          echo "--- Docker 关键配置项 ---"
          grep -E "^CONFIG_(NAMESPACES|NET_NS|PID_NS|IPC_NS|UTS_NS|MEMCG|OVERLAY_FS|VETH|BRIDGE|NF_NAT|SECCOMP|POSIX_MQUEUE|CGROUP_FREEZER|CGROUP_DEVICE|KEYS)=" .config | sort || true
          echo "════════════════════════════════════════════════"

          echo "status=success" >> ${GITHUB_OUTPUT}

      # ================================================================
      # ★ 步骤 7：编译内核（.config 已由上一步准备好，直接编译）
      # ================================================================
      - name: Compile kernel
        id: compile
        if: ${{ steps.injectconfig.outputs.status == 'success' && !cancelled() }}
        run: |
          cd /builder/kernel

          export CCACHE_DIR=/builder/kernel/.ccache
          export PATH="/usr/lib/ccache:$PATH"
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export DTC_FLAGS="-@"

          # 直接编译，.config 已由 injectconfig 步骤完整准备
          make -j$(nproc) Image.gz dtbs

          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Build deb packages
        id: debpkg
        if: ${{ steps.compile.outputs.status == 'success' && !cancelled() }}
        run: |
          cd /builder/kernel

          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-

          # Step 2: Build deb packages (headers + image)
          make -j$(nproc) bindeb-pkg LOCALVERSION=-msm8916 KDEB_PKGVERSION=${{ env.kernel_version }}-1 KDEB_SOURCENAME=linux-msm8916

          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Collect and package artifacts
        id: package
        if: ${{ steps.debpkg.outputs.status == 'success' && !cancelled() }}
        run: |
          cd /builder
          mkdir -p kernel_package
          cd kernel_package

          KERNEL_VER="${{ env.kernel_version }}"

          # Copy compiled kernel files
          cp /builder/kernel/arch/arm64/boot/dts/qcom/msm8916-thwc-ufi001c.dtb ./
          cp /builder/kernel/arch/arm64/boot/Image.gz ./

          # Find deb packages (bindeb-pkg outputs to parent dir /builder/)
          find /builder -maxdepth 1 -name "linux-headers-*${KERNEL_VER}*.deb" ! -name "*dbg*" -type f -exec cp {} ./linux-headers.deb \;
          find /builder -maxdepth 1 -name "linux-image-*${KERNEL_VER}*.deb" ! -name "*dbg*" -type f -exec cp {} ./linux-image.deb \;

          # Verify all files exist
          echo "=== Package contents ==="
          ls -la ./

          # Check required files
          for f in msm8916-thwc-ufi001c.dtb Image.gz linux-headers.deb linux-image.deb; do
            if [ ! -f "$f" ]; then
              echo "ERROR: Missing file: $f"
              exit 1
            fi
          done

          # Create tar.gz package
          tar -czf "${KERNEL_VER}.tar.gz" msm8916-thwc-ufi001c.dtb Image.gz linux-headers.deb linux-image.deb

          echo "package_path=/builder/kernel_package/${KERNEL_VER}.tar.gz" >> ${GITHUB_ENV}
          echo "package_name=${KERNEL_VER}.tar.gz" >> ${GITHUB_ENV}
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Upload Kernel to Release
        uses: ophub/upload-to-releases@main
        if: ${{ steps.package.outputs.status == 'success' && !cancelled() }}
        with:
          tag: kernel_stable
          artifacts: ${{ env.package_path }}
          allow_updates: true
          remove_artifacts: false
          replaces_artifacts: true
          make_latest: false
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            These are Mainline LTS(Long Term Support) kernels that provide stable support for Armbian and OpenWrt.
            这些是主线 LTS（长期支持）内核，为 Armbian 和 OpenWrt 提供稳定的支持。
            支持特性: DAE (eBPF 透明代理) + Docker 容器化
